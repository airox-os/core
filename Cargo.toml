[workspace]
resolver = "3"
members = [
  "bootloader",
  "kernel",
  "common",
  "userland/shell",
  "userland/utils",
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

# Removed [build] and [target] sections, move them to kernel/Cargo.toml